function Path_selecting_List = DP_sorting_search_FIM(new_Path_selecting_table)

% T = struct2table(new_Path_selecting_table); % convert the struct array to a table
% sortedT = sortrows(T, 'FIM_total_tr_value'); % sort the table
% sortedS = table2struct(sortedT); % change it back to struct array if necessary 
     
Path_selecting_List = new_Path_selecting_table;

 


